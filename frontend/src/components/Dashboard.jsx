import React from "react";
import { Box, Typography } from "@mui/material";

const Dashboard = () => {
  return (
    <Box>
      <Typography variant="body1">
        This is the main page of the application. You can navigate to other sections using the menu.
      </Typography>
    </Box>
  );
};

export default Dashboard;